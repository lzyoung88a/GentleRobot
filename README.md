# GentleRobot Studio

Research prototype for configuring and previewing visual-tactile robot responses in elder care scenarios.

## Run locally

```bash
cd three-demo
npm install
npm run dev
```

The current prototype uses vanilla JavaScript, Vite, Three.js, and a GLB robot model loaded from `public/models/gentle_robot_v0.glb`.
